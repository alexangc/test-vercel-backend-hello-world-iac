{
  "name": "test-vercel-backend-hello-world-iac",
  "version": "1.0.0",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node --watch ./src/devServer.js",
    "dev": "node --watch ./src/devServer.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tofu:plan": "./tmp/with_creds.sh tofu -chdir=\".infrastructure\" plan -var-file=env/production.tfvars",
    "tofu:apply": "./tmp/with_creds.sh tofu -chdir=\".infrastructure\" plan -var-file=env/production.tfvars"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
